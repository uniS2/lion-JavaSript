export function isNumericString(name){
  return isNaN(Number(name))
}

//^ isNaN
//^ isNaN은 JavaScript에서 숫자가 NaN(Not a Number)인지 확인하는 함수입니다. isNaN 함수는 매개변수로 숫자를 받으며, 숫자가 NaN이면 true를, 그렇지 않으면 false를 반환